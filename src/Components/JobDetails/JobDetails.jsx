// eslint-disable-next-line no-unused-vars
import React from 'react';

const JobDetails = () => {
    return (
        <div>
            nothing else
        </div>
    );
};

export default JobDetails;